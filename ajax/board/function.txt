1. 선언함수 - 이름이 있음

2. 익명함수 - = () => 

3. 대입함수 - 자스에서 함수는 객체이다. 

side convention 
1) 일급함수  - 함수를 다른 변수와 동일하게 다루는 언어
2) 고차함수 - 함수를 반환하는 함수를 고차 함수라고 부릅니다.

function foo = function(){
    console.log('hello');
}

함수가 이름을 가지고 있더라도 할당한 변수 이름을 사용해 함수를 호출할 수 있습니다. 
이름을 지정하면 코드를 디버깅할 때 유용합니다. 하지만 호출하는 방식에는 영향을 미치지 않습니다.

4. 즉시실행함수

콜백 자체는 익명이든 뭐든 될 수 있다.